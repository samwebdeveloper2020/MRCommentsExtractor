# Images Directory

This directory stores images downloaded from GitLab merge request comments.

The application automatically:

- Downloads images referenced in comments
- Saves them in this directory
- Displays them in the GUI image viewer

Image files are named based on their original filenames from GitLab.
